# Anthropic API Proxy Configuration
# Copy this file to .env and fill in your API keys

# API Keys
# ANTHROPIC_API_KEY=your-anthropic-api-key-here  # Optional, only needed if proxying to Anthropic models
OPENAI_API_KEY=your-openai-api-key-here          # Required if using OpenAI as fallback or default
GEMINI_API_KEY=your-google-ai-studio-api-key-here # Required if PREFERRED_PROVIDER=google

# Provider Configuration
PREFERRED_PROVIDER=openai  # Options: openai (default) or google

# Model Mapping Configuration
# These models will be used when Claude clients request haiku/sonnet
BIG_MODEL=gpt-4o           # Model to map Claude Sonnet requests to
SMALL_MODEL=gpt-4o-mini    # Model to map Claude Haiku requests to

# Alternative Google configuration:
# PREFERRED_PROVIDER=google
# BIG_MODEL=gemini-2.5-pro-preview-03-25
# SMALL_MODEL=gemini-2.0-flash
